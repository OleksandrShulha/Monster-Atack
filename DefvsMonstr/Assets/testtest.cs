using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class testtest : MonoBehaviour
{
    [SerializeField] Transform go;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
    //    if (Input.GetKeyDown(KeyCode.Space))
    //    {
    //        float curentdist = 1000f;
    //        List<GameObject> enemyes = new List<GameObject>(GameObject.FindGameObjectsWithTag("Enemy"));
    //        if (enemyes.Count > 0)
    //        {
    //            foreach (GameObject go in enemyes)
    //            {
    //                float dist = Vector3.Distance(go.transform.position, transform.position);
    //                if (dist < curentdist)
    //                {
    //                    curentdist = dist;
    //                    //Debug.Log(curentdist);
    //                }
    //            }

    //            //Debug.Log(enemyes.Count);
    //        }
    //        Debug.Log(curentdist);

    //        //float dist = Vector3.Distance(go.position, transform.position);
    //        //Debug.Log(dist);
    //    }
    }
}
